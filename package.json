{
  "name": "agenda-tecnica",
  "version": "1.0.0",
  "scripts": {
    "dev": "node start-dev.cjs",
    "server": "node server.cjs",
    "start": "node server.cjs",
    "start:production": "npm run build && node server.cjs",
    "test": "node --test tests/*.test.cjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0"
  }
}
